on: push

jobs:
  publish:
    name: 🚀 publish
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: "16.x"
          registry-url: "https://registry.npmjs.org"
      - run: npm install
      - run: npm ci
      - run: npm publish --access public
        env:
          NODE_AUTHçTOKEN: ${{ secrets.NPM_AUTH_TOKEN }}
