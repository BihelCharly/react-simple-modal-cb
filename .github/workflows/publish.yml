on: push

jobs:
  publish:
    name: 🚀 publish
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
        with:
          node-version: 10
      - run: npm install
      - run: npm test
      - name: 🚀 publish
      - run: npm publish
        with:
          token: ${{ secrets.NPM_AUTH_TOKEN }}
